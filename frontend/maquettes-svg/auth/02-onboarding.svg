<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 390 844" width="390" height="844">
  <defs>
    <style>
      .title { font-family: Arial, sans-serif; font-size: 28px; font-weight: bold; fill: #1E293B; }
      .body { font-family: Arial, sans-serif; font-size: 15px; fill: #64748B; }
      .btn-text { font-family: Arial, sans-serif; font-size: 18px; font-weight: 600; fill: #FFF; }
      .link { font-family: Arial, sans-serif; font-size: 14px; fill: #2563EB; font-weight: 600; }
      .link-gray { font-family: Arial, sans-serif; font-size: 14px; fill: #64748B; }
      .skip { font-family: Arial, sans-serif; font-size: 16px; fill: #64748B; }
    </style>
  </defs>

  <!-- Background -->
  <rect width="390" height="844" fill="#FFF"/>

  <!-- Status bar area -->
  <rect width="390" height="44" fill="#FFF"/>

  <!-- Skip button -->
  <text x="345" y="72" class="skip" text-anchor="end">Passer</text>

  <!-- Outer circle (restaurant icon - red theme) -->
  <circle cx="195" cy="280" r="80" fill="rgba(239,68,68,0.1)"/>
  <circle cx="195" cy="280" r="50" fill="rgba(239,68,68,0.2)"/>
  <!-- Restaurant icon -->
  <text x="195" y="295" font-family="Arial, sans-serif" font-size="40" text-anchor="middle" fill="#EF4444">🍽️</text>

  <!-- Title -->
  <text x="195" y="420" class="title" text-anchor="middle">Restaurants</text>

  <!-- Description -->
  <text x="195" y="455" class="body" text-anchor="middle">Réservez votre table en quelques clics</text>
  <text x="195" y="475" class="body" text-anchor="middle">dans les meilleurs restaurants de votre ville</text>

  <!-- Dot indicators -->
  <rect x="159" y="530" width="24" height="8" rx="4" fill="#2563EB"/>
  <circle cx="195" cy="534" r="4" fill="#E2E8F0"/>
  <circle cx="207" cy="534" r="4" fill="#E2E8F0"/>
  <circle cx="219" cy="534" r="4" fill="#E2E8F0"/>

  <!-- Suivant button -->
  <rect x="24" y="680" width="342" height="56" rx="16" fill="#2563EB"/>
  <text x="195" y="714" class="btn-text" text-anchor="middle">Suivant</text>

  <!-- Already have an account -->
  <text x="145" y="770" class="link-gray">Déjà un compte ? </text>
  <text x="258" y="770" class="link">Se connecter</text>
</svg>
